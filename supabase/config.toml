# Local development config for the Supabase CLI.
# Docs: https://supabase.com/docs/guides/local-development

project_id = "wec-championship-tracker"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public", "wec"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 17

[db.seed]
# `supabase db reset` runs the migrations, then loads these seed files.
enabled = true
sql_paths = ["./seed.sql"]

[studio]
enabled = true
port = 54323
api_url = "http://127.0.0.1:54321"

# Local email testing inbox (magic links land here). Open the URL below in a
# browser after requesting a sign-in link.
[inbucket]
enabled = true
port = 54324

[auth]
enabled = true
# The base site URL and the redirect targets the magic link / OAuth flow are
# allowed to use.
site_url = "https://wec-zeta.vercel.app"
additional_redirect_urls = [
  "https://wec-zeta.vercel.app/auth/confirm",
  "https://wec-zeta.vercel.app/auth/callback",
  "http://localhost:3000/auth/confirm",
  "http://localhost:3000/auth/callback",
  "http://127.0.0.1:3000/auth/confirm",
  "http://127.0.0.1:3000/auth/callback",
]
jwt_expiry = 3600
enable_signup = true

[auth.email]
enable_signup = true
# No real SMTP locally — confirmation links are captured by Inbucket/Mailpit.
enable_confirmations = false
secure_password_change = false

# Override the default magic-link email so the link points straight at the
# Next.js confirm route with a token_hash. The default template uses
# {{ .ConfirmationURL }}, which redirects with a PKCE ?code= param that the
# token_hash-based /auth/confirm route can't verify (it returns error=invalid).
[auth.email.template.magic_link]
content_path = "./supabase/templates/magic_link.html"

# Google OAuth. This block only configures the LOCAL Supabase CLI stack — the
# hosted project's Google provider is configured in the Supabase Dashboard
# (Authentication -> Providers), not here.
#
# Local Google sign-in needs real Google Cloud OAuth credentials, so it's off
# by default: magic-link sign-in works locally with zero setup (links land in
# Inbucket at http://127.0.0.1:54324). To test Google locally, set
# SUPABASE_AUTH_GOOGLE_CLIENT_ID / SUPABASE_AUTH_GOOGLE_SECRET in .env.local,
# flip `enabled` to true, and restart `npm run db:start`. In the Google Cloud
# console add this authorized redirect URI:
#   http://127.0.0.1:54321/auth/v1/callback
[auth.external.google]
enabled = false
client_id = "env(SUPABASE_AUTH_GOOGLE_CLIENT_ID)"
secret = "env(SUPABASE_AUTH_GOOGLE_SECRET)"

# This app uses Next.js server actions/route handlers, not Supabase Edge
# Functions, so the Deno edge runtime container is disabled. It also avoids a
# startup-time network fetch from deno.land.
[edge_runtime]
enabled = false

[analytics]
enabled = false
